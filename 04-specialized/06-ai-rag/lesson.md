# AI / LLM / RAG

مفاهیم: token، prompt، embedding، vector search، retrieval، context، generation و evaluation.

معماری RAG: Ingest → Chunk → Embed → Index → Retrieve → Generate → Evaluate.

کلیدهای API باید از environment variable خوانده شوند و هرگز در Git commit نشوند.

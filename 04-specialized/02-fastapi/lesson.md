# FastAPI Track

HTTP، REST، routing، Pydantic validation، dependency injection، OpenAPI، authentication، JWT، database و testing.

Capstone این بخش: API مدیریت فروش با endpointهای CRUD و تست integration.

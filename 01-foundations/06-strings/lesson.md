# درس 06 — Strings

موضوعات: indexing، slicing، immutability، `strip`، `split`، `join`، `replace`، `find` و f-string.

```python
course = "Python Academy"
print(course[0:6])
print(course.lower())
```

تمرین: نام کامل کاربر را پاک‌سازی کنید و تعداد کلمات آن را نمایش دهید.

# درس 08 — Functions

تعریف تابع، parameter، argument، return، scope، default arguments، keyword arguments، `*args` و `**kwargs`.

```python
def total(price: float, quantity: int = 1) -> float:
    return price * quantity
```

قاعده آموزشی: تابع باید یک مسئولیت روشن داشته باشد و نام آن رفتار را توضیح دهد.

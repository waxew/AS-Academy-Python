# Generator & Decorator

Generator با `yield` داده را lazy تولید می‌کند. Decorator رفتار تابع یا کلاس را بدون تغییر مستقیم بدنه آن توسعه می‌دهد.

کاربردهای عملی: پردازش فایل بزرگ، logging، timing و authorization در برنامه‌های وب.

# Comprehensions

List/Set/Dictionary comprehensions روش فشرده ساخت collection هستند.

```python
squares = [number ** 2 for number in range(1, 11)]
evens = {number for number in range(20) if number % 2 == 0}
```

خوانایی بر کوتاه‌نویسی اولویت دارد.
